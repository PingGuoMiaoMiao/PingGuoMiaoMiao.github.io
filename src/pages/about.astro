---
import BaseLayout from '../layouts/BaseLayout.astro';
const pageTitle = "关于我";
const identity = {
  firstName: "莎拉",
  country: "加拿大",
  occupation: "技术撰稿人",
  hobbies: ["摄影", "观鸟", "棒球"],
};
const skills = ["HTML", "CSS", "JavaScript", "React", "Astro", "Writing Docs"];
---

<BaseLayout pageTitle={pageTitle}>
  <section class="about">
    <article class="card">
      <h2 class="gradientText">你好，我是{identity.firstName}</h2>
      <p>来自{identity.country} 的 {identity.occupation}，热爱 {identity.hobbies.join("、")}。</p>
      <p>这个站点用 Astro 构建，用来记录学习与生活点滴。</p>
      <div class="skills">
        {skills.map((s) => <span class="chip">{s}</span>)}
      </div>
    </article>
  </section>
</BaseLayout>
<style>
  .about { margin-top: .5rem; }
  .card {
    background: rgba(0,0,0,.25);
    border-radius: 16px;
    padding: 1rem 1.25rem 1.25rem;
    box-shadow: 0 6px 20px rgba(0,0,0,.18), inset 0 0 0 1px rgba(255,255,255,.06);
  }
  .skills { display: flex; flex-wrap: wrap; gap: .5rem; margin-top: .75rem; }
  .chip { padding: .3rem .6rem; background: rgba(255,255,255,.06); border-radius: 999px; font-size: .9rem; }
</style>
