# 苹果喵喵Blog - Astro 博客系统

## 📋 项目概述

这是一个基于 Astro 构建的现代化博客系统，完全使用本地 Markdown 文件作为内容源，支持动态组件渲染、SEO 优化、RSS 订阅等功能。

## ✨ 主要特性

- 📝 **本地 Markdown 内容**：所有文章存储在 `/src/content/posts/` 目录
- 🧩 **动态组件系统**：支持在 Markdown 中引用 Astro 组件
- 🏷️ **标签系统**：文章分类和标签页
- 📊 **SEO 优化**：自动生成 OpenGraph、Twitter Card
- 📡 **RSS 订阅**：自动生成 RSS Feed
- 🗺️ **Sitemap**：自动生成网站地图
- 💬 **评论系统**：基于 localStorage 的客户端评论（可扩展为后端）
- 📈 **阅读统计**：基于 localStorage 的阅读量统计
- 🎨 **现代化 UI**：参考 demo-front_bend 风格

## 📁 目录结构

```
src/
├── components/          # 可复用组件
│   ├── Background_Carousel.astro
│   ├── PostCard.astro
│   ├── Comments.astro
│   └── ...
├── content/
│   └── posts/          # Markdown 文章
│       ├── 1.md
│       ├── 2.md
│       └── ...
├── layouts/            # 页面布局
│   ├── BaseLayout.astro
│   └── PostLayout.astro
├── pages/              # 路由页面
│   ├── index.astro     # 首页
│   ├── post/
│   │   └── [id].astro  # 文章详情页
│   ├── tag/
│   │   └── [tag].astro # 标签页
│   ├── tags/
│   │   └── index.astro # 标签云
│   ├── rss.xml.js      # RSS Feed
│   └── sitemap.xml.ts  # Sitemap
└── utils/              # 工具函数
    ├── markdown.ts     # Markdown 解析
    ├── posts.ts        # 文章处理
    └── comments.ts     # 评论系统
```

## 📝 Markdown 文章格式

每篇文章必须包含以下 frontmatter：

```yaml
---
title: "文章标题"
tag: "标签名称"
date: "2025-11-19"
cover: "/images/xxx.png"  # 可选：封面图
summary: "文章简介"       # 可选
status: "published"       # published 或 draft
readingTime: 3            # 阅读时间（分钟）
---

文章内容...

@Background_Carousel.astro (1-8)
```

### 组件引用语法

在 Markdown 中使用 `@ComponentName.astro (startLine-endLine)` 来引用组件：

```markdown
这是一段文字。

@Background_Carousel.astro (1-8)

继续写内容...
```

系统会自动：
1. 解析组件引用
2. 动态加载对应组件
3. 传入 `startLine` 和 `endLine` 作为 props
4. 在对应位置渲染组件

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

### 构建

```bash
npm run build
```

### 预览

```bash
npm run preview
```

## 📄 页面路由

- `/` - 首页（文章列表）
- `/post/[id]` - 文章详情页
- `/tag/[tag]` - 标签页
- `/tags` - 标签云
- `/rss.xml` - RSS Feed
- `/sitemap.xml` - Sitemap

## 🎨 自定义组件

### 创建新组件

1. 在 `src/components/` 创建组件文件，如 `MyComponent.astro`
2. 组件可以接收 `startLine` 和 `endLine` props：

```astro
---
interface Props {
  startLine?: number;
  endLine?: number;
}

const { startLine, endLine } = Astro.props;
---

<div>
  <p>这是一个自定义组件</p>
  {startLine && endLine && (
    <p>引用了第 {startLine} 到 {endLine} 行</p>
  )}
</div>
```

3. 在 Markdown 中使用：

```markdown
@MyComponent.astro (1-10)
```

## 🔧 配置

### 更新网站 URL

编辑 `astro.config.mjs`：

```js
export default defineConfig({
  site: "https://your-domain.com",
  // ...
});
```

### 自定义样式

编辑 `src/styles/global.css` 来修改全局样式。

## 📦 功能扩展

### 评论系统

当前使用 localStorage 存储评论，可以扩展为：

1. 使用第三方服务（如 Giscus、Utterances）
2. 连接后端 API
3. 使用数据库存储

修改 `src/components/Comments.astro` 和 `src/utils/comments.ts`。

### 阅读统计

当前使用 localStorage 统计阅读量，可以扩展为：

1. 连接后端 API 记录真实访问
2. 使用分析服务（如 Google Analytics）

## 🐛 常见问题

### 组件无法加载

确保：
1. 组件文件存在于 `src/components/`
2. 组件名称大小写匹配
3. 组件导出为默认导出

### RSS 不工作

检查：
1. `@astrojs/rss` 已安装
2. `src/pages/rss.xml.js` 文件存在
3. 文章 frontmatter 格式正确

## 📝 注意事项

1. **移除登录/注册**：本项目已完全移除用户认证系统
2. **本地内容**：所有文章内容来自本地 Markdown 文件
3. **组件渲染**：组件在服务端渲染，确保组件支持 SSR
4. **SEO**：确保填写完整的 frontmatter 以获得最佳 SEO 效果

## 📄 许可证

MIT

